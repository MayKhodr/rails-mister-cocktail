<h1>Cocktails</h1>

<div class="container p-3" >

  <h2>Drinks:</h2>

  <div class='cards'>
    <% @cocktails.each do |cocktail| %>
      <div class="card" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_tag cocktail.photo.key, height: 300, width: 400, crop: :fill %>')">
        <h3 cçass="drink-name"> <%= link_to cocktail.name, cocktail_path(cocktail) %> </h3>
      </div>
    <% end %>
    <%= link_to 'Add a Cocktail', new_cocktail_path %>
  </div>
</div>
